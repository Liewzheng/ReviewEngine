// Prettier flat config (prettier.config.js).
// Align with the repo's .editorconfig (indent 4 / 2 for yaml).
export default {
  singleQuote: true,
  trailingComma: 'es5',
  tabWidth: 2,
  semi: true,
  printWidth: 100,
};
